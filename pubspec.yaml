name: party_chaos
description: "A fun party games app with multiplayer support."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase: ^2.5.0
  provider: ^6.1.2
  go_router: ^14.2.0
  shared_preferences: ^2.2.3
  uuid: ^4.4.0
  equatable: ^2.0.5
  flutter_animate: ^4.5.0
  confetti: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
